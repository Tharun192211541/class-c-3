#include<stdio.h>
#include<string.h>
void natural()
{
  int n,rem,sum=0;
  printf("enter the number");
  scanf("%d",&n);
  printf("sum of the digits is:");
  while (n>0)
  {
  rem=n%10;
  sum=sum+rem;
  n=n/10;
}
printf("%d",sum);

}
int main()
{
	natural();
}
